// Crie uma variável nota e escreva um if/else que imprima "Aprovado" se a nota for maior ou igual a 6, e "Reprovado" caso contrário.

const nota = 10;

if (nota >= 7) {
    console.log("Aprovado!");
} else {
    console.log("Reprovado!");
}