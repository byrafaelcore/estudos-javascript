// Crie uma verificação de "usuário pode entrar na balada": a pessoa precisa ter idade >= 18 e temIngresso === true. Monte essa condição com variáveis e operadores lógicos, imprimindo true ou false conforme os valores mudam.

let idade = 23;
let temIngresso = false;

console.log(idade >= 18 && temIngresso === true);